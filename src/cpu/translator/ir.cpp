#include "cpu/translator/ir.h"
// ir.h es puramente de datos (structs/enums) -- no hace falta logica aqui
// todavia. El fichero se mantiene para que la biblioteca tenga una unidad
// de traduccion asociada al header y CMake no se queje de una lib vacia.
