# cmake/ -- reservado

Vacío a propósito por ahora. Si el proyecto necesita módulos CMake propios
más adelante (p. ej. para automatizar la extracción/precompilación de
shaders Cg del roadmap de `docs/GRAPHICS.md`), van aquí en vez de en el
`CMakeLists.txt` raíz.
